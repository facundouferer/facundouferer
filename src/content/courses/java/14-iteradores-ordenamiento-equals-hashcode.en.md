---
course: 'java'
slug: '14-iteradores-ordenamiento-equals-hashcode'
title: 'Iterators, Sorting, and equals/hashCode Contract'
description: 'Master safe collection iteration, custom sorting with Comparable/Comparator, and the equals/hashCode contract.'
order: 14
lang: 'en'
published: true
---

# Iterators, Sorting, and equals/hashCode Contract

## 1. Iterator
Safe modification during iteration.

## 2. Comparable vs. Comparator
`Comparable` (natural ordering) vs `Comparator` (custom strategy).

## 3. equals() & hashCode() Contract
Required for hash-based collections.

## 4. Hands-on Exercise
Create a `Book` class with `equals()` and `hashCode()` preventing duplicates in `HashSet`.
