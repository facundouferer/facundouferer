---
course: 'java'
slug: '14-iteradores-ordenamiento-equals-hashcode'
title: 'Iteradores, Ordenamiento y Contrato equals/hashCode'
description: 'Dominá la iteración segura, el ordenamiento personalizado con Comparable/Comparator y el contrato equals y hashCode.'
order: 15
lang: 'es'
published: true
---

# Iteradores, Ordenamiento y Contrato equals/hashCode

## 1. Iterator
Modificación segura durante el recorrido.

## 2. Comparable vs. Comparator
`Comparable` (orden natural) vs `Comparator` (orden personalizado).

## 3. Contrato equals() y hashCode()
Obligatorio para colecciones basadas en hash.

## 4. Ejercicio Práctico
Creá una clase `Libro` implementando `equals()` y `hashCode()` para evitar duplicados en un `HashSet`.
