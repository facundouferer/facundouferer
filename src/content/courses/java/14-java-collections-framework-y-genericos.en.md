---
course: 'java'
slug: '13-java-collections-framework-y-genericos'
title: 'Java Collections Framework and Generics'
description: 'Explore Java Collections Framework: Set, List, Queue, Map interfaces and standard implementations.'
order: 14
lang: 'en'
published: true
---

# Java Collections Framework and Generics

## 1. Interfaces and Implementations
`List` (`ArrayList`), `Set` (`HashSet`), `Queue` (`PriorityQueue`), `Map` (`HashMap`).

## 2. Generics (`<T>`)
Compile-time type safety.

## 3. Hands-on Exercise
Use `Map<String, Integer>` to count word frequencies.
